我给你的最终判定

  如果你的目标是“结束 step-04”
  我同意结束。

  我建议你采用的表述
  你可以把最终状态理解为：

  最终验证通过（带两条保留意见）

  两条保留意见
    1. Story 5.5c 更适合作为发布门禁/验收清单，而非标准开发 Story
    2. FR-046 关于“安装后通过配置修改语言设置”的表达仍可更显式


  ────────────────────────────────────────────────────────────────────────────────────────────────────────────────

  最简结论版

  你这次修改是有效的，尤其是：

    • 4.6 拆分成功
    • 5.5 拆分成功
    • Epic 1 的 Foundation Sprint 定位合理

  所以我会给：

  通过。

  但我会在评审备注里补一句：

    │ 当前 epics.md 已可进入实施；仅需注意 5.5c 更偏发布门禁项而非标准开发 Story，且 FR-046 
    │ 的后半句仍以隐含覆盖为主。

---

FR-046 关于“安装后通过配置修改语言设置”的表达仍可更显式，以下是建议的一个最直接、最显式的版本

  建议在 Story 5.5a 里 补一条 AC：

    • Given 用户已完成安装，且 config.json 中的 language 从 zh-CN 修改为 en
    • When 再次执行任意用户可见输出的命令（如 npx aiforge --dry-run）
    • Then 系统使用英文输出，而无需重新运行 aiforge init（FR-046）

  为什么这一条更好

  因为它把原来“隐含成立”的部分，直接说死了：

    • 安装后
    • 通过配置修改
    • 无需重新初始化
    • 修改后立即生效

  这四件事一旦写进 AC，FR-046 就不再是“推断覆盖”，而是 显式覆盖。

  如果你想更严一点

  还可以再补半条：

    • And 当 language 配置值非法或不支持时，系统回退到默认中文输出并给出提示

  但如果你现在只想补 一个最小且最有力的 AC，上面那条就够了。

完整版 Story 5.5a

  下面这版可以直接作为 完整、显式、可验收 的 Story 5.5a 参考。

  Story 5.5a: 国际化语言选择与配置

  As a 用户,
  I want 在初始化时选择界面语言，并能在安装后通过配置修改语言设置,
  So that 中英文用户都能舒适使用，且后续无需重新初始化即可切换输出语言。

  Acceptance Criteria

    • Given 用户在 aiforge init 中选择语言
    When 选择英文
    Then 后续所有用户可见输出使用英文（FR-046）
    And 语言设置保存到 config.json 的 language 字段

    • Given 默认配置
    When 未设置语言
    Then 系统默认使用中文输出（NFR-U1）

    • Given data/messages.ts
    When 检查实现
    Then 所有用户可见字符串通过 messages 模块获取
    And 支持根据 language 配置切换中英文文案

    • Given 用户已完成安装，且 config.json 中的 language 从 zh-CN 修改为 en
    When 再次执行任意包含用户可见输出的命令（如 npx aiforge --dry-run）
    Then 系统使用英文输出
    And 无需重新运行 aiforge init（FR-046）

    • Given config.json 中的 language 配置值非法、缺失或不受支持
    When 系统加载语言配置
    Then 自动回退到默认中文输出
    And 给出明确提示，说明当前语言配置无效并已使用默认语言

  我的判断

  这版比之前强在两点：

    • 把 “安装后通过配置修改语言设置” 写成了显式 AC
    • 补上了 非法配置回退策略，避免实现时各写各的

  如果你愿意，我下一步可以继续帮你把 Story 5.5b / 5.5c 也顺手统一润色到同一风格。

  下面我把 Story 5.5b / 5.5c 也统一成同一风格

  我会顺手把两个问题一起处理掉：

    • 5.5b 写得更像可执行的测试 Story
    • 5.5c 写得更像可审计的发布门禁项


  ────────────────────────────────────────────────────────────────

  Story 5.5b: 端到端集成测试

  As a 开发者,
  I want 端到端集成测试覆盖核心安装流程、安装模式和目标平台,
  So that 在发布前能够自动验证系统整体可用性，并及时发现回归问题。

  Acceptance Criteria

    • Given 一个符合约定目录结构的模拟知识仓库
    When 执行全局安装和项目级安装
    Then 4 个目标工具（Copilot、Claude、Cursor、VS Code）的安装流程均可成功完成
    And 安装结果与规则映射一致

    • Given 三种安装模式
    When 分别执行复制模式、符号链接模式和 flatten 模式
    Then 三种模式均通过端到端验证
    And 各自输出符合预期目标路径和文件形态

    • Given --dry-run 模式
    When 执行端到端测试
    Then 不发生任何文件写入、复制、覆盖或创建行为
    And dry-run 输出的安装计划与实际安装结果一致（NFR-U5）

    • Given 存在文件冲突场景
    When 执行安装测试
    Then 系统触发冲突处理流程
    And 备份、跳过、覆盖等行为符合设计预期

    • Given 安装结果为零项的场景
    When 执行端到端测试
    Then 系统触发零结果诊断输出
    And 输出包含扫描路径、匹配结果和修复建议

    • Given macOS 环境
    When 执行端到端测试
    Then 所有测试通过（NFR-C1）

    • Given Linux 环境（CI）
    When 执行端到端测试
    Then 所有测试通过（NFR-C2）

    • Given 端到端测试完成
    When 查看测试结果
    Then 能明确区分通过项、失败项及失败原因
    And 失败结果可作为发布阻塞依据


  ───────────────────────────────────────────────────────────────────────────

  Story 5.5c: MVP Go/No-Go 发布门禁验收

  先说清楚：这个更像发布验收项，不像标准开发 Story。
  但如果你决定保留在 epics.md，我建议写成下面这样，至少它会更清晰、更可审计。

  As a PM / QA,
  I want 一份明确的 MVP 发布门禁清单并逐项完成验证,
  So that 团队对产品是否达到发布标准有客观、一致、可追溯的判断。

  Acceptance Criteria

    • Given MVP Go/No-Go 门禁清单
    When 逐项执行发布前验证
    Then 以下条件必须全部满足：
       • 新员工首次配置旅程可完整走通
       • --dry-run 输出与实际安装结果一致
       • 冲突场景存在备份保护
       • 零结果场景存在诊断输出
       • npm 包中不包含公司域名、仓库地址、Token 或其他敏感信息（NFR-S1）

    • Given 端到端验证结果、关键旅程验证结果和发布检查结果
    When 汇总发布评审材料
    Then 每一项门禁都有明确状态：通过 / 未通过 / 阻塞
    And 每个未通过项都附带对应风险说明和处理建议

    • Given 存在任一未通过的 blocker 项
    When 执行 Go/No-Go 评审
    Then 发布结论为 No-Go
    And 不进入正式发布

    • Given 所有 blocker 项均已关闭
    When 执行 Go/No-Go 评审
    Then 发布结论为 Go
    And 形成可追溯的发布验收记录

    • Given 发布门禁验收完成
    When 复盘本次验收结果
    Then 团队可以明确知道 MVP 当前的已知风险、剩余限制和上线边界


  ──────────────────────────────────────────────────────────────

  我的建议

  5.5b
  这版已经像一个合格的测试 Story 了，没问题。

  5.5c
  如果你追求文档纯度，我还是建议你未来把它从 Story 体系里拿出去，改成：

    • Release Checklist
    • Go/No-Go Checklist
    • MVP Acceptance Gate

  但如果你现在想保持 epics.md 不再大动，这版可以接受。


  ────────────────────────────────────────────────────

  最终推荐的 5.5 结构

    • 5.5a：国际化语言选择与配置
    • 5.5b：端到端集成测试
    • 5.5c：MVP Go/No-Go 发布门禁验收

  这个拆法已经比原版合理很多。